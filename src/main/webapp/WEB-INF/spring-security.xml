<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:beans="http://www.springframework.org/schema/beans"
       xmlns:jee="http://www.springframework.org/schema/jee"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/security
       http://www.springframework.org/schema/security/spring-security.xsd">

    <http pattern="/resources/**" security="none" />
    <http pattern="/login" security="none" />
    <http pattern="/register" security="none" />

    <http auto-config="true">
        <intercept-url pattern="/admin**" access="ROLE_ADMIN" requires-channel="https" />
        <intercept-url pattern="/**" access="ROLE_USER" requires-channel="https" />
        <form-login
                login-page="/login"
                authentication-success-handler-ref="chatAuthenticationHandler"
                authentication-failure-handler-ref="chatAuthenticationHandler"
                username-parameter="username"
                password-parameter="password" />
        <logout logout-success-url="/login?logout" />
    </http>

    <beans:bean id="chatAuthenticationHandler" class="de.deyovi.chat.web.security.ChatAuthenticationHandler" >
        <beans:property name="chatUserService" ref="chatUserService" />
        <beans:property name="chatUserSession" ref="chatUserSession" />
    </beans:bean>

    <authentication-manager >
        <authentication-provider user-service-ref="chatUserService" />
    </authentication-manager>

</beans:beans>